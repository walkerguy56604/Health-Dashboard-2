import pandas as pd
import matplotlib.pyplot as plt
from datetime import datetime

# Load your CSV
df = pd.read_csv('daily_activity_log.csv')

# Convert Date column to datetime
df['Date'] = pd.to_datetime(df['Date'])

# Plotting
plt.figure(figsize=(10,6))
plt.plot(df['Date'], df['BP Systolic'], marker='o', label='Systolic')
plt.plot(df['Date'], df['BP Diastolic'], marker='o', label='Diastolic')
plt.xlabel('Date')
plt.ylabel('Blood Pressure (mmHg)')
plt.title('Daily Blood Pressure Trend')
plt.legend()
plt.grid(True)
plt.tight_layout()
plt.savefig('daily_activity_plot.png')
plt.show()
